// ==============================================================================
// conf/base.config — Default resource allocation profiles
// ==============================================================================

// TODO (Phase 6): process.cpus/memory/time defaults, errorStrategy with retry,
// labels (process_low, process_medium, process_high) and the report/dag/timeline
// directives will be wired in here during Phase 6.
